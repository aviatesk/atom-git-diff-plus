describe('The integration with Status-Bar package', () => {
  // @TODO: Write specs for the Status-Bar integration
  // - `StatusBarController` object
  //      * can show diffs in status-bar
  //            + show `added`, `modified` and `deleted` lines for a modified file
  //            + show only `added` lines for an newly file
  //            + show only `ignored` status for an ignored file
  //      * can update diffs for switched editor
  //      * clears diffs for editors under non-.git directory and non editors
  console.log(`Specs for the Status-Bar integration have not be done yet`);
});
