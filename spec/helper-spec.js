describe('Helper methods', () => {
  // @TODO: Write specs for helper methods
  // - `repositoryForEditor`
  //      * Can find a repository in current projects
  //      * Can find a repository outside current projects
  //      * Don't cause error when .git repository is deleter
  //      * Can recognize an newly added .git repository after `initializeRepositoryCache`
  //      * Time check for each call ?
  console.log(`Specs for helper methods have not be done yet`);
});
